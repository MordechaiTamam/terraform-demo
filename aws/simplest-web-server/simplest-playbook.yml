- name: Data Manipulation
  hosts: web-servers
  gather_facts: true
  vars:
   my_name: devops-expert
  tasks:
  - name: Install latest version of "python3.7"
    shell: echo "<p> From Ansibleeeeee! </p>" >> /var/www/html/index.html
    become_user: root